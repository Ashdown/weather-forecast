(function (React, App) {
    App.Components.Generator = React.createClass({

        render: function () {

            return (
                <section>
                    <a href="#">Generate forecast</a>
                </section>
                );

        }
    });

})(React, App);